# import torch
# from os.path import exists

# # check if use GPU
# device = torch.device("cuda" if torch.cuda.is_available() else "cpu")

# # load the dataset
# if exists('dataset/recent_x.hdf5') and exists('dataset/recent_y.hdf5'):